# Fashion-MNIST-Classification-using-PyTorch
This notebook classifies clothing items from the Fashion MNIST dataset using an MLP (Multi-Layer Perceptron) model built with PyTorch nn; and summarizes results in a confusion matrix.
